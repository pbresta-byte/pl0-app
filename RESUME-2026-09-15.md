## UPDATE 2026-09-15 (remote-control session, later still): status + open questions

Three commits landed this session (all on `main`, all verified via the jsdom
harness described further down before committing):
- `f6e2e7e` — Directives A/B/C (Remedies removal, Cross-Varga removal, Horary
  consonant-collision fix). See the "UPDATE 2026-09-15 (later session)"
  section below for full detail.
- `52e6b9d` — Trikālagnāna Yoga fix. Re-derived from B.V. Raman's own
  primary-source text (`domains/raman-300-combinations.md`, yoga #234,
  already ingested) after finding the prior implementation — sourced from
  two secondary web forums — had two real bugs: (1) ANDed all sub-conditions
  together instead of Raman's actual OR-of-two-conditions structure
  (verified via a synthetic unit test: own-navamsa+Mridvamsha true, no
  aspect → old code said absent, Raman's own text doesn't require an aspect
  for that path, new code correctly says present), and (2) "Gopuramsa" was
  computed against the wrong varga set (the general 10-varga Dashavarga
  dignity ladder, not Raman's specific D1+D3+D4+D7 four-varga definition —
  verified against his own worked example, Jupiter 1st degree of Dhanus).
  Both conditions independently unit-tested; full Natal page render with
  real birth data confirmed zero exceptions.

**Open question 1 — Shadbala doesn't exist in this codebase.** Directive 3
("suppress a yoga unless... Shadbata [confirms it]") assumed a working
Shadbala (six-fold planetary strength) system already existed as a signal to
check. Grepped the whole file — it doesn't; nothing for Sthāna/Dig/Kāla/
Cheṣṭā/Naisargika/Dṛik Bala. Building one for real (to the same
verify-against-JHora rigor this codebase already holds itself to elsewhere)
is itself a multi-hour sub-project, on top of the rest of directive 3. Asked
the user: build it for real, or ship a Shadbala-free "lite" version of
directive 3 now and track Shadbala separately.

**Open question 2 — directive 3 doesn't generalize as a blanket rule.**
6 of the 9 currently-implemented Jñāna-series yogas (Dhīmantaḥ, Sādhana-
Vidyā, Tapasvi, Bahuvidyā, Vidyā-Vināśa, Grantha-Kartā) are, per their own
cited classical sourcing already in the code, D1-only house-lord
combinations — their source verses never ask for D9/varga/Shadbala
confirmation. Forcing a multi-chart gate onto them would invent a stricter
rule the texts don't state, not fix a bug. Trikālagnāna (just fixed) is the
one yoga in the current set that actually is classically multi-chart. So
directives 2–3's real remaining shape is "implement more of Raman's 300
yogas, each held to whatever rigor ITS OWN source text demands" — not a
generic suppression gate retrofitted onto what already exists. The 300-list
only has full extracted detail for Trikālagnāna (#234) right now; the other
299 are name+line-citation stubs needing the same extract-then-implement
treatment before any of them are real. That's open-ended by nature — asked
the user which/how many yogas to prioritize next rather than guessing.

**Hermes book-ingestion trial (in progress, job_mu3kjd65 as of this
writing).** Given the prior session's 5/5 Hermes book-ingestion failures
(see "Book ingestion — messy, mostly failed" section below), ran a
diagnostic first: asked Hermes directly whether it had a stored memory
entry matching the stale "sync the working copy forward / B.V. Raman 300 /
Trikalagnana" task. It reported no such entry, but did report having
memory of "PL0 app version sync notes (v1.61/v1.62 vs v1.66)" and "working
directory paths for pl0-app repositories" (plural — consistent with the
decoy-folder confusion already documented below) — and answered with ZERO
tool calls, i.e. purely from context, not an actual memory search. Also
observed the underlying model backing Hermes fell back through two broken
providers (nvidia/nemotron-3-super-120b free tier → llama-3.1-nemotron-
ultra-253b → finally poolside/laguna-xs-2.1) within a single ~70s response —
real backend instability that's a plausible contributor to erratic task-
following, separate from any memory issue. Also note: an early one-line
diagnostic prompt got truncated to just its first sentence by the Hermes
bridge, and it answered only that fragment — worth knowing if a future
prompt to Hermes seems to get a non-sequitur reply; keep prompts short and
un-line-broken where possible, or verify receipt before trusting a "did it
understand" assumption.

Given that, launched ONE real trial task via `ask_hermes_async`: ingest
Vishnu Bhaskar's *Advanced Techniques of Predictive Astrology* Vol.1
(source: `C:\Users\drrag\OneDrive\Desktop\jyotish-corpus\Jyotish_2013_
Vishnu Bhaskar_Advanced Techniques of Predictive Astrology_Vol.1 of 2_MUST
READ.txt`) into `domains/vishnu-bhaskar-predictive-astrology-vol1.md`,
explicit real workdir, explicit instruction to disregard any remembered
stale task, `raman-300-combinations.md` given as the structural template.
**Verdict on `job_mu3kjd65` (2026-09-16): Hermes correctly followed the
prompt this time — no stale-memory pollution, right working directory, read
the right source file and the template file, started the correct grep for
yoga content — but the job died 16 minutes in without writing anything,
because its own backend model infrastructure is broken: two dead-model 404s
(`nvidia/nemotron-3-super-120b-a12b:free` doesn't exist in the provider's
catalog anymore; a second fallback model 404'd on account/function lookup),
then a rate limit, then three straight connection-error timeouts (up to
~728s / 12 min each) on a fourth fallback model, totally unrelated to
prompt quality or memory pollution.** This is conclusive: the earlier 5/5
failures were plausibly THIS SAME backend instability all along, not (or
not only) a stale-memory bug — a broken/exhausted model config can produce
exactly the kind of garbled, ignores-the-actual-task behavior that looked
like "pulled a stale task from memory." **Decision: don't keep retrying
Hermes against a broken backend — someone needs to fix Hermes's own model/
provider config (dead model IDs, exhausted quotas) before it's usable
again. Doing the remaining 7 books directly instead**, same as the (c)
fallback already chosen for the code directives.

Not touched this session: book ingestion beyond the one trial, APK
build/push question. Directives A/B/C/Trikālagnāna work above superseded
the "Directive A, B, C" summary that used to be right below this line —
see this section instead for current status.

## UPDATE 2026-09-16: Shadbala module + Raman 300-yoga engine started

User confirmed both open questions from the previous update: (1) Shadbala
should be built for real but kept as a standalone side module, not wired
into the app; (2) implement ALL 300 of Raman's yogas, no cherry-picking.

**Shadbala**: new file `shadbala.js` at the repo root (commit `7406b78`).
Zero dependency on index.html/DOM — pure functions over a `pos` object
shaped exactly like `computePositions()` already produces. Implements all
six BPHS ch.27 components. File header is explicit about which parts are
solidly verified (28 unit tests covering every checkable boundary) vs.
standard-convention-but-unverified (no live reference oracle was
available — JHora is installed on this machine but is GUI-only, no
automation tool for it existed this session). Read the file header before
trusting any of the "standard convention" pieces for something high-stakes.

**Raman's 300 Combinations engine**: new panel on the Natal page, separate
array from JNANA_YOGAS (different book/numbering). **17 of 300 implemented
as of commit `49281b6`**: #1-10 and #12-18 (#11 deferred — needs Gulika's
zodiacal position, which doesn't exist anywhere in this app; only a
Muhurta time-window "Gulika" check exists, not a rasi placement). Every
single entry was checked against Raman's own Definition **and** Remarks,
not the one-liner alone — this caught two real misreadings before they
shipped: Adhi Yoga's Definition reads as needing all of houses 6+7+8
simultaneously, but the Remarks and worked Chart No.12 explicitly rule
that out (any 1-3 of the three benefics, count-graded); Dhurdhura/
Ubhayachari need BOTH sides of the reference point at once, not either
alone, per their own Remarks' worked examples. Two honesty-flagged
ambiguities logged in code comments rather than silently resolved:
Parvata Yoga's benefic-in-kendra count (no explicit number given, unlike
Adhi), and Kahala Yoga's "strongly disposed" threshold for the Lagna lord
(operationalized as own/exalted/friend dignity).

Commits this session, in order: `f6e2e7e` (directives A/B/C), `52e6b9d`
(Trikalagnana fix), `7406b78` (shadbala.js), `95e2576` (Raman #1-7),
`89eb1a4` (Raman #8-10,12), `4233fc6` (Raman #13-16), `49281b6` (Raman
#17-18). All verified via the jsdom harness before committing (see method
description further up this file).

**Honest pace note for whoever picks this up**: 17/300 at this rate,
holding every entry to the Definition+Remarks discipline above, took a
very substantial single session. 300 is realistically many more sessions
of work — there is no shortcut that preserves the rigor without also
losing real per-yoga nuance (a generic data-driven rule engine was
considered and rejected as a first move, since the 300 entries are
genuinely heterogeneous in structure — many will still end up needing
their own small bespoke function, same as the ones done so far).

**UPDATE (same session, continued): 46/300 now implemented** (commits
`95e2576` through `6d0d770`, most recently batch 6 covering #29-47). User
explicitly confirmed: keep going past 17, target all 300, no cherry-picking
pace concerns raised. Batches 5-6 covered the five Panchamahapurusha Yogas
(#19-23, Lagna-kendra + own-or-exalt per graha, correctly NOT reusing the
old removed Lagna-or-Moon-kendra logic), Budha-Aditya/Mahabhagya/Pushkala/
Lakshmi/Gauri (#24-28), and Bharathi/Chapa/Sreenatha/all-12-Malika/Sankha/
Bheri/Mridanga/Parijatha (#29-47). Established a reusable dispositor-chain
coding pattern (find a house's lord, find where it sits, follow its
Navamsa lord, etc.) that made the later entries in each batch faster than
the earlier ones — expect this compounding to continue.

**UPDATE (same session, still continuing): 73/300 implemented** as of
commit `06795ab` (batches 7-10 covered #48-74: Gaja/Kalanidhi/Amsavatara/
Harihara Brahma/Kusuma, Matsya/Kurma/Devendra/Makuta/Chandika/Jaya, Vidyut/
Gandharva/Siva/Vishnu/Brahma/Indra/Ravi, and Garuda/Go/Gola/Thrilochana/
Kutuvardhana plus the first 4 of 32 Nabhasa Yogas — Yupa/Ishu/Sakti/Danda).
User said explicitly to keep going past 17 — "all 300 like the movie" —
confirmed no pace concerns, just keep implementing.

New reusable helpers added along the way: `isTrikonaFrom(refSid, testSid)`
(mutual-trine check, sibling to `isKendraFrom`), the Nabhasa four-house
generic helper pattern (`nabhasaFourFoldYoga`, same shape as the earlier
12-way `malikaYoga` helper). `readDateTime("b")` is now called directly at
RAMAN_YOGAS build time (not threaded as a parameter) whenever a yoga needs
raw birth date/time/lat/lon for day-night or sunrise/sunset math (Garuda,
Mahabhagya) — cheap, idempotent, matches how the rest of this codebase
already re-derives rather than threads extra params.

**UPDATE (same session, still continuing): 106/300 implemented** as of
commit `2ed4725` (batches 10-13 finished ALL 32 of Varaha Mihira's Nabhasa
Yogas, #71-102, plus Duryoga/Daridra/Harsha/Sarala/Vimala #103-107). This
closes out the entire Nabhasa section — a major structural chunk of the
book. Two more genuine name collisions found and handled the same way as
before (disambiguated by entry number only): #78 "Chapa Yoga" vs. #30, #82
"Sakata Yoga" vs. #12, #97 "Gola Yoga" vs. #68.

New reusable pattern this stretch: `countDistinctSignsOccupied(pos)` for
the 7 Sankhya yogas (how many distinct signs the 7 classical grahas
occupy, no house reference needed at all); `twoHouseConfinementYoga(pos,
ascSignIdx, houseA, houseB)` for "all grahas confined to exactly these two
houses, both used" (Gada/Sakata-new/Vihaga); the `nabhasaSevenFoldYoga`/
`nabhasaFourFoldYoga` helpers both directly reuse `malikaYoga` under the
hood — worth remembering that pattern (N grahas across N contiguous houses
from a start point) shows up repeatedly under different names across this
book and is always the same underlying check.

**Immediate next step when resuming**: continue sequentially from #108
(right after Vimala Yoga, txt continues past L5465 — re-grep raw txt for
"108." to find exact line). Past the Nabhasa section, entries return to the
simpler house-lord/graha-combination style seen in #1-70 — expect faster
per-yoga throughput than the Nabhasa stretch. Same verification discipline
throughout: read Definition+Remarks (not just the one-liner), implement,
jsdom-verify against real chart data via `run_raman_yogas_check.js` in the
scratchpad, commit each batch of ~5-15. Reusable helpers now available:
`lordOfHouse(houseNum, ascSignIdx)`, `isKendraFrom`, `isTrikonaFrom`,
`bsHouseFromAsc`, `bsGrahasInHouses`, `dignityTierInSign`,
`aspectsSignWholeSign`, `RAMAN_BENEFICS`, `countDistinctSignsOccupied`,
`twoHouseConfinementYoga`, `malikaYoga` (reusable for any N-contiguous-
houses-from-start-point pattern). "Powerful"/"strongly disposed" (Raman's
own recurring undefined-threshold language) has been consistently
operationalized throughout as own/exalted/friend dignity — keep that
convention unless a specific entry's Remarks give a clearer threshold.

# RESUME — 2026-09-15 session (paused mid-work at user's request)

Handoff note for whoever picks this up next — a future Claude Code session, or
Hermes. User asked to pause here ("pause here until later" / "stop here for
now") while three code directives were about to be hand-edited directly.
**No edits have landed in `www/index.html` yet — the file is untouched**
(verified via `git diff` immediately before this note was written).

## Repo location — READ THIS FIRST

- **Real repo (canonical, git-tracked):** `E:\ClaudeWorkSpaceAnthropic\CloneOfpl0app\pl0-app`
  (branch `main`, last commit `057e3f4` "v1.66"). All work described below
  targets this path.
- **Decoy, do not use:** `E:\ClaudeWorkSpaceAnthropic\pl0-app\` — a separate,
  non-git Obsidian-vault working folder that happens to also be named
  "pl0-app" and also has a `domains/` subfolder. Multiple Hermes jobs this
  session wrote into this folder by mistake despite explicit workdir
  instructions pointing at the real repo. Its copy of `www/index.html` is
  stale (v1.61-level per one job's own exploration — missing yoga detectors
  the real repo already has). Never treat it as a source of truth.

## Original task (from user, framed as a "Supervisor(Claude)/Core Engine(Hermes)" directive)

Five directives against `www/index.html` (single-file app, ~12,729 lines,
no build step):

1. **Remove "Remedies and Combinations" section** entirely (nav, page,
   render fn, i18n keys) — frontend only, don't touch shared `GRAHA_REMEDY`
   data tables if referenced elsewhere (they are — see "Poison zone"
   feature).
2. **Consolidate Yoga readings**: ingest B.V. Raman's 300 core yoga
   combinations, merge the "General" page's yoga panel into one seamless
   reading (not fragmented paragraphs), and fix "Cristian's Calculation"
   bug — criteria wrongly failing/dismissing valid yogas — aligning the
   filtering logic with traditional Vedic rules.
3. **Strict multi-chart conditional rendering for yogas** — hide a yoga
   unless it's 100% applicable across D1/D9/relevant vargas, exact degrees,
   houses, and Shadbala — suppress if any cross-chart criterion fails.
4. **Remove "Cross-Yoga Aggregate" section** (the "Cross-Varga Yoga
   Aggregate" panel) — purge from computation and UI.
5. **Debug Prashna (Horary) section** — currently yields empty results /
   fails silently; find root cause, fix chart initialization and data
   mapping into the UI.

Plus, added mid-session: ingest 9 more jyotish books (see "Book ingestion"
below) with structured extraction (yoga rules, calculation methods,
calc-verification material, stats/case studies, numbers/colors/
significators/tags), and an ask for an ETA on pushing an updated APK with
the finalized yoga section (yogas-applicable-only display in General page).

## Sequencing decision (mine, as Supervisor)

Split into: (A) three well-scoped, lower-risk code edits — directives 1, 4,
5 above — to do first and review before touching the much larger, riskier
yoga-engine rework (directives 2–3). Book ingestion for source material was
run in parallel since it doesn't touch app code.

## Exact code anchors for the three pending directives (confirmed current, not yet edited)

**Directive A — Remove Remedies & Combinations:**
- Nav button: `www/index.html:735`
- Page container `#pageRemedies`: `www/index.html:924`
- i18n key `navRemedies`: `www/index.html:1049`
- Page registry entry: `www/index.html:8223`
- State var `remediesSelectedGraha` + `setRemediesGraha()`: `www/index.html:10706-10707`
- Render fn `renderRemediesPage()`: starts `www/index.html:10999` (uses
  `remediesSelectedGraha` at 11007; body continues past this, exact end line
  not yet re-confirmed — re-grep for the next top-level `function ` after
  10999, or matching closing brace, before editing)
- ⚠️ Do NOT delete `GRAHA_REMEDY` / `GRAHA_REMEDY_ES` data tables — a
  different feature (Poison-zone, ~line 10079 in the version explored
  earlier) reads them; only delete what's exclusive to this page.

**Directive B — Remove Cross-Yoga Aggregate:**
- Panel `<div class="panel">` titled "Cross-Varga Yoga Aggregate": was at
  `www/index.html:8621-8652` (an IIFE computing per-graha dignity tallies
  across all 16 vargas + Mahapurusha reinforcement verdict) — re-grep before
  editing, line numbers may have drifted since first located.
- The "Yoga combinations" panel just above it (~8609-8618) has a hint
  sentence explicitly referencing "the Cross-Varga Yoga Aggregate panel
  below" (~line 8611) — update/remove that cross-reference once the panel
  is gone.
- Check whether `tally`, `MAHAPURUSHA_YOGA`, `dignityTierInSign`,
  `vargaSignForKey` are used elsewhere before deleting anything beyond the
  panel's own block — likely shared utilities, keep them.

**Directive C — Debug Prashna/Horary:**
- This app's horary system is **Sarvatobhadra-Chakra (SBC)**, syllable→
  nakshatra/pada keyed — NOT a Tajika/Varshaphala system (those Tajika refs
  elsewhere, ~5008/5448/5459, belong to the separate annual/Varshaphala
  page — don't conflate).
- Nav button `data-page="pageHorary"`: `www/index.html:732`
- Container `#pageHorary`: `www/index.html:932`
- Render dispatch `if (pageId === 'pageHorary') renderHoraryPage();`:
  `www/index.html:8249`
- Bulk of SBC logic starts: `www/index.html:11167` (`// ---- Horary /
  Praśna (Sarvato Bhadra Chakra) ----`)
- State vars `horaryQuestionText`, `horarySelectedKey`: `www/index.html:11173-11174`
- **Not yet investigated**: the actual root cause of empty/silent-failure
  results. Next step is to trace `renderHoraryPage()` end-to-end (early
  return? swallowed exception? shape mismatch between the built SBC matrix
  object and what the template reads from `horaryPageContent`?). No fix
  attempted yet.

## Yoga-engine work (directives 2–3) — not started, but source material exists

`domains/raman-300-combinations.md` (already in the real repo, 464 lines,
22KB) has a full 300-yoga inventory with `txt-line` citations back to
`Jyotish_ 300 important combinations_B.V. Raman.txt`, plus a detailed
Trikalagnana Yoga breakdown (#234) that is directly load-bearing for the
"Cristian's Calculation" bug:

- Trikalagnana requires checking Jupiter's position in **Navamsa (D-9)**,
  not just Rasi (D-1) — a D-1-only aspect scan will never detect it.
- Needs **Shashtiamsa (D-60)** arithmetic for the Mrudwamsa sub-division
  check (odd/even sign → Shashtiamsa span → Mrudwamsa sub-span).
- "Gopuramsa" = own-varga-4-times = check Rasi + D-3 + D-4 + D-7 for
  Jupiter's ownership in each — a genuine multi-varga traversal.
- Kala Sarpa Yoga is a separate Appendix, not one of the numbered 300 — the
  engine's `yoga_type` classifier should tag it `appendix:kala_sarpa`
  rather than as entry 1–300.

`domains/jataka-yoga-remedies.md`'s header has **not** been updated yet to
cite `raman-300-combinations.md` — do that when starting the real
yoga-engine work.

## Book ingestion — messy, mostly failed

User asked to ingest, in addition to the above: Vishnu Bhaskar *Advanced
Techniques of Predictive Astrology* Vol.1, Om Prakash Paliwal *Parashar
Code of Prediction*, Hora Ratnam (Santhanam), Jataka Parijata Vol.1
(two OCR passes) + Vol.2, Brahma Sphuta Siddhanta Vol.1–3, and Noel Tyl's
*Solar Arcs* (flagged as Western/tropical — do not merge into sidereal
yoga criteria without a deliberate conversion decision). All source texts
already have pre-OCR'd `.txt` extracts in
`C:\Users\drrag\OneDrive\Desktop\jyotish-corpus\` (382KB–1.27MB each, no
need to touch the PDFs in `C:\Users\drrag\ansel\1050 jyotish books with
siderial zodiac\`).

**Status: only one ingestion job actually did real, verifiable work all
session** (`job_mu315a1u`, ~22 min, 81 tool calls) — but it ingested the
WRONG book (B.V. Raman's 300 Combinations, already covered above) into the
wrong location. I salvaged its output by copying it into
`domains/raman-300-combinations.md` in the real repo.

**Every other book-ingestion job this session (4 of them, for Vishnu
Bhaskar, Parashar Code, Hora Ratnam, and Jataka Parijata Vol.1) silently
ignored its assigned task and independently re-did the SAME B.V. Raman
300-Combinations ingestion again**, each inventing a different storage
convention and writing to a different wrong location:
- `internal-os/projects/pl0-app/domain-raman-300-combinations.md`
- `internal-os/knowledge/raw/jyotish/` + `internal-os/knowledge/frameworks/`
- Seven scattered artifacts: `skills/`, `.opencode/skills/`,
  `.claude/agents/`, an Obsidian note in the decoy `pl0-app/` folder
- A whole bootstrapped wiki (`internal-os/knowledge/SCHEMA.md`, `index.md`,
  `log.md`, `concepts/`, `entities/`)

One job's own transcript said outright: *"My task from the memory: sync
the working copy forward, add B.V. Raman's 300 Combinations, fix
Trikalagnana..."* — it pulled a stale task from Hermes's own persistent
memory and ran that instead of the prompt it was actually given, regardless
of what the prompt said. This happened 5/5 times across 5 differently-worded
prompts. **This is not something retrying or rewording fixes.**

None of these 5 jobs touched the real repo (`E:\ClaudeWorkSpaceAnthropic\
CloneOfpl0app\pl0-app`) — verified clean via `git diff`/`git status`
immediately after. No damage, but no progress either: Vishnu Bhaskar,
Parashar Code, Hora Ratnam, and Jataka Parijata Vol.1 are all **still not
ingested anywhere**. Jataka Parijata Vol.2, Brahma Sphuta Siddhanta
(Vol.1–3, planned as one combined ingestion), and Solar Arcs were queued
as "batch 2" but never dispatched, given the pattern above.

**Open question for the user, asked but not yet resolved**: given
Hermes ignored 5/5 assigned tasks in favor of a remembered one, do we (a)
have the user check Hermes's own memory/config for what's pinning it to
that stale task, (b) keep trying to delegate to Hermes with even more
explicit prompts, or (c) do book ingestion directly instead (Claude
reading the `.txt` files and writing the `domains/*.md` files itself,
same as the code directives). User chose, for the three code directives
specifically, **(c)-equivalent: Claude does it directly** rather than
delegating further to Hermes. The book-ingestion approach question is
still open — not yet decided for Vishnu Bhaskar / Parashar Code / Hora
Ratnam / Jataka Parijata Vol.1 / Vol.2 / Brahma Sphuta Siddhanta / Solar
Arcs.

## APK build/push — unresolved, needs input before any ETA

Checked the Android/Capacitor toolchain in this environment:
- `capacitor.config.json` exists (`appId: com.pbresta.muhurtagauge`,
  `appName: PL0`, `webDir: www`).
- Java 21 (Temurin) present; `android/gradlew` + `gradlew.bat` present.
- **`ANDROID_HOME` / `ANDROID_SDK_ROOT` are not set** in this shell — an
  Android SDK may or may not exist elsewhere on the machine; not checked
  further.
- **No signing keystore found** anywhere under `android/`.
- **"Push" is ambiguous** — could mean handing over a debug APK to sideload,
  or a signed release build (which needs the user's own keystore/
  credentials — not something Claude/Hermes can generate). Needs
  clarification before any build attempt or ETA.

## Immediate next steps (in order, when resumed)

1. Re-grep the three Directive A/B/C anchors above (line numbers likely
   still accurate but confirm before editing) and make the edits directly
   via Edit tool — no Hermes.
2. For Directive C, actually trace `renderHoraryPage()` and find the real
   root cause before "fixing" anything (nothing has been diagnosed yet
   beyond locating the relevant code region).
3. Review the diff as Supervisor/QA before considering this phase done.
4. Decide how to handle the still-pending book ingestions (see open
   question above) before attempting the big yoga-engine rework
   (directives 2–3), which should also fold in `domains/
   raman-300-combinations.md`'s Trikalagnana findings once available.
5. Get clarification from the user on APK "push" meaning + locate/confirm
   an Android SDK before estimating that step.

## UPDATE 2026-09-15 (later session): Directives A/B/C done, verified in jsdom

Re-grepped all anchors above — line numbers had barely drifted, all
confirmed current before editing. Made the edits directly via Edit tool (no
Hermes), then went further than "read the diff" for verification: since
Claude Code has no GUI here and no Chrome extension was connected this
session, I extracted the real `www/index.html` into a `jsdom` sandbox
(installed locally, not a repo dependency) and actually *ran* the app —
`showPage()`, typed input, clicked-equivalent function calls — the same way
a user's browser would, rather than trusting static reading alone.

**Directive A (Remove Remedies & Combinations) — done.** Removed the nav
button, `#pageRemedies` container, i18n key, page-registry entry,
`remediesSelectedGraha`/`setRemediesGraha`, `mahapurushaActiveInChart()`
(orphaned once its only caller was gone), and `renderRemediesPage()` in
full. Left `GRAHA_REMEDY`/`GRAHA_REMEDY_ES` and `MAHAPURUSHA_YOGA` alone —
confirmed `GRAHA_REMEDY*` are still read by the Anatomy page's Poison-zone
panel, and `MAHAPURUSHA_YOGA` is inert data likely to be reused by the
yoga-engine rework (directives 2–3). Also fixed a dangling "see Remedies
page for the full table" cross-reference in that same Poison-zone panel,
since that page no longer exists.

**Directive B (Remove Cross-Yoga Aggregate) — done.** Removed the
"Cross-Varga Yoga Aggregate" panel (the per-graha 16-varga dignity-tally
IIFE) from `renderNatalPage()`, and removed the "Yoga combinations" panel's
hint sentence that pointed down at it. `dignityTierInSign`/`vargaSignForKey`
confirmed still used elsewhere (line ~5172/5229) — kept, untouched.

**Directive C (Debug Prashna/Horary) — investigated end-to-end, but the
originally-reported symptom did not reproduce.** I could not get "empty
results / fails silently" to happen anywhere: I swept all 112 consonant
entries across all 28 stars through `castHorary()`, plus edge cases (empty
input, whitespace, digits, symbols, unicode, a no-match letter, tropical
ayanamsa mode, Abhijit unchecked) — every single one rendered a correct,
non-empty result or the correct intentional "type a question" / "no
syllable matches" message. Zero exceptions, zero blank panels.

**What I did find and fix instead — a real, confirmed "data mapping" bug**
(so this still satisfies the directive's own wording): 8 consonants (Na,
Tha, Da, Ja, Gha, Do, Du, De) are printed under **more than one star** in
the source's own pada table (e.g. "Na" appears under Ardra pada-3, Hasta
pada-3, Anuradha pada-1, *and* U.Bhadrapada pada-4). The old
`PRASNA_SYLLABLE_INDEX` was a plain object keyed by lowercased consonant,
so on each collision the later star in the build loop silently overwrote
the earlier one — any question starting with one of those 8 consonants
always auto-detected to whichever star happened to be last (U.Bhadrapada,
Revati, or Abhijit, empirically), regardless of which one the source table
actually intends for that opening syllable. Worse: the **manual
"pick the syllable directly" dropdown was equally broken** — its
`<option value>` was also just the bare lowercased consonant, so selecting
"Na — Hasta (pada 3)" from the dropdown produced the exact same
(wrong, overwritten) result as selecting "Na — Ardra (pada 3)" or any of
the other two — the four options were visually distinct but functionally
identical. That's arguably a stronger match for "silent failure" than a
blank screen: you pick a specific answer and silently get a different one.

Fix: `PRASNA_SYLLABLE_INDEX` now maps each consonant to an *array* of every
star+pada it's printed under (was a single overwritten object). Added
`PRASNA_KEY_INDEX`, keyed by `"Star::pada"` (unique per option), for
resolving an explicit dropdown pick. Dropdown `<option value>` changed from
the bare consonant to `"Star::pada"` so all four "Na" options are now
distinguishable and each resolves to the star the user actually picked —
verified this for all 4 "Na" candidates. When auto-detect hits one of the 8
ambiguous consonants, the page now shows an explicit "printed under more
than one star: X, Y, Z, W — pick the one that applies below" message
instead of silently guessing, and does not render a (possibly wrong)
result until the user disambiguates via the dropdown. Unambiguous
consonants (100 of the 108) are completely unaffected — verified via the
same full sweep, byte-identical behavior to before the fix.

**Open question for the user**: since I couldn't reproduce the literal
"empty results" symptom, it's worth checking whether what you saw was (a)
this ambiguous-consonant issue read as "not working," (b) something
specific to the Android APK/WebView build rather than a browser (a stale
build, given this session's earlier confusion between the real repo and
the decoy `pl0-app` folder, is also plausible), or (c) something I haven't
hit yet — if you can reproduce it again post-fix, the exact question text
and whether it was browser or APK would narrow it down fast.

Verification method used throughout (for future sessions): `npm install
jsdom` in a scratch directory (not added to this repo — it's a throwaway
test harness, not a dependency), then `new JSDOM(html, {runScripts:
'dangerously', resources:'usable'})` to get a real DOM + running the app's
actual inline scripts, then drive it via `win.showPage(...)`,
`doc.getElementById(...).value = ...`, `win.castHorary()` etc. exactly like
a user would, reading `container.innerHTML` back out. This is a far
stronger verification than reading the diff, and is the technique to reach
for on this repo going forward given there's no build step and no
attached browser this session.

**Not done, still next**: directives 2–3 (yoga-engine consolidation —
ingest B.V. Raman's 300 combinations, fix "Cristian's Calculation," strict
multi-chart conditional rendering), the book-ingestion approach decision,
and the APK-push clarification. All exactly as described above — nothing
about today's work changes what's needed there.

## UPDATE 2026-09-16 (fresh session, continuing from 121/300): now 185/300 implemented

Picked up exactly where the prior session left off (commit `b47f5c9`,
"batch 14," 121/300) and continued yoga #123 onward sequentially. **185 of
300 now implemented**, across commits `12da3d8` through `9aca737` (batches
15–21, all on `main`, all verified via the jsdom harness — see
`run_raman_yogas_check.js`-style scratch script, described further up this
file — before every commit):

- `12da3d8` batch 15: #123-132 (10 yogas) — a second Lagna-based Dhana Yoga
  family (#123-128) plus Bahudravyarjana and the three Swaveeryaddhana
  yogas.
- `a599d6b` batch 16: #133-143 (11 yogas) — Madhya/Anthya Vayasi Dhana,
  Balya Dhana, the two Bhratrumooladdhana yogas, and five more
  relative-specific wealth combinations (mother/sons/enemies/spouse/
  lifelong/effortless).
- `cd4bcce` batch 17: #144-153 (10 yogas) — all ten Daridra (poverty)
  Yogas, the negative mirror of the Dhana run.
- `4158dfb` batch 18: #154-160 (7 yogas) — speech/eloquence combinations
  plus Bhaskara and Marud (two pure mutual-sign-distance yogas, explicitly
  Lagna-independent per Raman's own Remarks).
- `11a9fee` batch 19: #161-171 (11 yogas) — fame (Saraswathi, Budha),
  face/appearance (Mooka, Netranasa, Andha, the two Sumukha, the two
  Durmukha), and food (Bhojana Soukhya, Annadana).
- `54e7b59` batch 20: #172-182, **#174 skipped** (10 yogas implemented) —
  food/dependency, speech defects, sibling-count combinations.
- `9aca737` batch 21: #183-184, #186-188, **#185 skipped** (5 yogas
  implemented) — courage/valour and property combinations.

**Three entries skipped this session** (same treatment as #11 from the
prior session — noted in the panel's own intro-text hint, not added to
`RAMAN_YOGAS`):
- **#174 (Sarpaganda Yoga)** — sole condition is "Rahu joins the 2nd with
  Mandi"; Mandi is a Gulika-family point this app has never computed
  (same gap as #11).
- **#185 (Yuddhatpaschaddrudha Yoga)** — one AND-ed clause needs a
  benefic/"cruel" (Rakshasa) classification of the 60 individual D60
  Shashtiamsa divisions. This app has a numeric D60 *ordinal* computation
  (`mridvamshaOrdinal`, used by an unrelated yoga) but no verified
  per-amsa deity/temperament table, and no verified source was available
  this session to build one safely. Unlike a missing OR-branch (see #158/
  #169 below), a missing AND-clause can't just be omitted without
  systematically over-reporting presence — so this one is a real skip, not
  a partial implementation.
- Two OTHER entries (**#158 Jada**, **#169 Durmukha II**) also touch the
  same missing-Mandi/Gulika gap but were still implemented, because in
  both cases the Mandi-dependent clause is one branch of an OR with an
  independently-checkable alternate path — that branch is simply omitted
  from the OR (never asserted false), not treated as grounds to skip the
  whole entry.

**New reusable helper**: `ownVargaCount(graha, pos, navPos, setKey)` — an
own-sign-ONLY counterpart to the existing `vaiseshikamsaCount()` (which
also credits Exalted placements). Needed for #183 (Yuddha Praveena Yoga),
whose "own vargas" phrasing is specifically about own-sign occupation, not
exaltation — reuse this instead of `vaiseshikamsaCount()` wherever a
yoga's text says "own" and specifically excludes exaltation.

**Genuine Definition-vs-Remarks/worked-example divergences resolved this
session** (the single most valuable thing to read if auditing judgment
calls):
- **#139 (Putramooladdhana)**: Definition requires the Lagna lord in
  Vaiseshikamsa; Raman's own Chart No.74 explicitly says "the Lagna lord
  is NOT in Vaiseshikamsa but he is decidedly strong" while still treating
  the chart as illustrating the yoga — implemented as Vaiseshikamsa OR
  strong dignity (an OR, not the Definition's strict AND). **#140**
  (immediately next), by contrast, has its own Remarks explicitly
  REAFFIRM the strict Vaiseshikamsa requirement — kept as a strict AND.
  Same-family yogas, opposite resolution, each cited to its own text.
- **#172 (Parannabhojana)**: Definition ANDs "debilitated/unfriendly
  Navamsa" with "aspected by a debilitated planet"; Remarks split this
  into a base result (Navamsa condition alone) and a worse escalation
  (adding the aspect) — implemented per the Remarks, aspect tracked as a
  secondary field only.
- **#180 (Dwadasa Sahodara)**: Raman calls the literal Definition
  "confounding" and hand-reduces it (working through all 12 Lagnas) to:
  3rd lord in a kendra, Mars exalted in Capricorn conjunct Jupiter, with
  Capricorn a trikona from the 3rd lord. Since Capricorn is simultaneously
  Jupiter's debilitation sign, the worked Chart No.82 explicitly needs
  (and gets) Neecha Bhanga to cancel that debility — implemented as a real
  requirement, using the existing `nichaBhangaCheck()` helper, not
  incidental chart color.
- **#186 (Satkathadisravana)**: Definition ANDs "3rd house is a benefic
  sign" with "3rd lord in a benefic amsa"; Remarks say the house-sign
  clause is outright irrelevant — implemented per the simplified Remarks
  reading.
- **#164 (Netranasa)**: Chart No.78 is offered as an explicit "slight
  variation" on the Definition (6th lord ASPECTING the Lagna instead of
  occupying it, still counted) — folded in as an OR, but only for the 6th
  lord specifically (the chart keeps the 2nd/10th lords in strict
  occupation).

**Naming collisions found this session** (same "let the #N prefix
disambiguate" treatment as before): none new beyond what the prior
session already logged — #123-132's second Dhana Yoga family and #130-132's
Swaveeryaddhana yogas were named with continuing Roman numerals (VI-XI,
I-III) rather than colliding names, since the book itself doesn't name
#123-128 individually.

**Immediate next step when resuming**: continue sequentially from **#189**.
Re-grep the raw text (`grep -n "^189\." "C:\Users\drrag\OneDrive\Desktop\
jyotish-corpus\Jyotish_ 300 important combinations_B.V. Raman.txt"`) since
OCR noise shifts line offsets unpredictably — the citation table in
`domains/raman-300-combinations.md` is navigation-only, not a substitute
for reading Definition+Remarks in the raw text (per the standing project
discipline). Same verification loop as always: implement a batch of
~5-15, run the jsdom scratch script against two charts, confirm zero load
errors + correct entry count (previous total + batch size, minus any
skips) + no `undefined` in spot-checked output, THEN commit with
`git add www/index.html` only (never `-A`/`.`  — this repo has many
unrelated untracked files sitting in it). Bump the intro-text yoga-count
string (`grep -n "#1-10, #12-173"` to find it — the exact substring
changes every batch, re-grep rather than assume) in both English and
Spanish on every batch.

Reusable helpers available (superset of what the prior session's note
listed): all of `lordOfHouse`, `isKendraFrom`, `isTrikonaFrom`,
`bsHouseFromAsc`, `bsGrahasInHouses`, `dignityTierInSign`,
`aspectsSignWholeSign`, `isOwnOrExalt`, `norm360`, `SIGNS`, `SIGN_LORD`,
`GRAHA_LIST`, `MALEFIC_GRAHAS`, `RAMAN_BENEFICS`,
`countDistinctSignsOccupied`, `twoHouseConfinementYoga`, `malikaYoga`,
`vaiseshikamsaCount(graha, pos, navPos, setKey)` (setKey one of
`'shad'|'sapta'|'dasa'|'shodasa'`; named-title lookup is by EXACT count
via `VAISESHIKAMSA_NAMES[setKey][count]`, so "attains Vaiseshikamsa" with
no specific named tier given has been read throughout as count>=2 — "any
named title" — while a SPECIFIC named tier like "Parvatamsa" or
"Gopuramsa" needs an exact count match, e.g. `===4` or `===6`, not `>=`),
the new `ownVargaCount` (own-sign only, see above), `navamsaSidereal(sid)`,
`nichaBhangaCheck(graha, siderealLong, pos, ascSignIdx)` (returns `null`
if not debilitated, else `{cancelled, cond1, cond2, dispositor,
exaltedHere}`), and the new-this-session `housesCountFrom(fromSignIdx,
toSignIdx)` (1-12 sign-distance helper, "Nth from X" phrasing — used by
Bhaskara/Marud #159-160, reusable for any future yoga phrased the same
way). "Powerful"/"strongly disposed"/"strong" continues to be
own/exalted/friend dignity tier throughout; Shadbala (the standalone
`shadbala.js` module) remains deliberately unwired from `index.html` per
explicit earlier instruction — do not wire it in without asking first,
keep using the dignity-tier proxy as this whole project has consistently
done.

## UPDATE 2026-09-16 04:00 EDT — Hermes ingest batch launched (6 remaining books)

`job_mu3rvbhk` (Parashar Code of Prediction) confirmed DONE and verified on
disk: `domains/parashar-code-of-prediction.md` (256 lines). Provider fix
held — job recovered through nemotron->step->longcat fallback chain instead
of dying.

Fired all 6 remaining books async, fire-and-forget (not actively polling —
conserving usage per user instruction):

| job_id | book | target file |
|---|---|---|
| job_mu3ta18s | Vishnu Bhaskar, Advanced Techniques Vol.1 | domains/vishnu-bhaskar-advanced-techniques-vol1.md |
| job_mu3ta42u | Hora Ratnam (Santhanam) | domains/hora-ratnam-santhanam.md |
| job_mu3taa2v | Jataka Parijata Vol.1 | domains/jataka-parijata-vol1.md |
| job_mu3tacs5 | Jataka Parijata Vol.2 | domains/jataka-parijata-vol2.md |
| job_mu3tajd6 | Brahma Sphuta Siddhanta (Vol.1-4 combined — corpus has 4 vols, not 3 as earlier noted) | domains/brahma-sphuta-siddhanta.md |
| job_mu3tanj1 | Solar Arcs (Noel Tyl) — Western/tropical, explicitly reference-only | domains/solar-arcs-noel-tyl-REFERENCE-ONLY.md |

Each prompt included a note asking Hermes to prefer stepfun/step-3.7-flash
or meituan/longcat-2.0 over nemotron if model selection is configurable —
no MCP-level model param exists on `ask_hermes_async`, so this is a soft
request, not a guarantee.

Check status via `mcp__hermes__check_hermes_job(job_id)` for each, and
verify the output file actually exists with `wc -l` before trusting
"done" status — that's the pattern that caught nothing wrong so far, but
stays the standard.

## UPDATE 2026-09-16 ~04:40 EDT — Hermes fallback chain now has a paid failsafe

User topped up $10 OpenRouter credit. Configured directly (no destructive
changes, CLI's `fallback add` is TUI-only so edited config/env files
directly instead — both confirmed live via `hermes fallback list` /
`hermes status` afterward):

- `C:\Users\drrag\AppData\Local\hermes\.env` — OPENROUTER_API_KEY updated
  to the new key tied to the $10 balance (ends `...772a`).
- `C:\Users\drrag\AppData\Local\hermes\config.yaml` — `fallback_providers`
  now 3 entries, tried in order when primary fails (rate-limit/5xx/conn
  errors):
  1. `google/gemini-2.5-flash` via **openrouter** (paid, 1M context,
     $0.30/M in $2.50/M out — verified live against OpenRouter's own
     /v1/models endpoint before writing)
  2. `stepfun/step-3.7-flash` via nous (free, unchanged)
  3. `meituan/longcat-2.0:free` via nous (free, unchanged)

Primary model deliberately left unchanged (`upstage/solar-pro4:free` via
nous) — `hermes insights --days 7` showed ~218.5M tokens/week of actual
usage, all currently free-tier. Switching the primary to a paid model at
that volume would burn ~$9.70/week and drain the $10 in about a week for
no real gain, since most of that volume is routine terminal/skill work
that doesn't need a paid model. Putting the paid model on fallback instead
means the $10 only gets spent on the subset of requests that were already
failing (dead/rate-limited free models) — which is the actual problem
this was meant to solve (see job_mu3rvbhk's earlier fallback chain: dead
nemotron -> step -> longcat, all free/nous, all congested).

Not yet done: did not re-kick the 6 in-flight book-ingestion jobs
(job_mu3ta18s / job_mu3ta42u / job_mu3taa2v / job_mu3tacs5 / job_mu3tajd6 /
job_mu3tanj1) against the new fallback config — they were already running
before this change. Next status check should verify each against its
target file with `wc -l`, not just trust job status (per established
practice this session).

## UPDATE 2026-09-16 06:38 EDT — END OF DAY HANDOFF — resume only on explicit user command

### Book ingestion batch status (7 total)
1. Parashar Code of Prediction — DONE, verified (`domains/parashar-code-of-prediction.md`, 256 lines)
2. Vishnu Bhaskar, Advanced Techniques Vol.1 — DONE, verified (`domains/vishnu-bhaskar-advanced-techniques-vol1.md`, 101 lines)
3. Hora Ratnam (Santhanam) — DONE, verified (`domains/hora-ratnam-santhanam.md`, 34 lines — legitimately short, book has no worked chart examples, confirmed by the extraction agent, not a failure)
4. Jataka Parijata Vol.1 — DONE, verified (`domains/jataka-parijata-vol1.md`, 292 lines)
5. Jataka Parijata Vol.2 — DONE, verified (`domains/jataka-parijata-vol2.md`, 311 lines)
6. Brahma Sphuta Siddhanta (4 vols) — IN PROGRESS, split into 4 per-volume jobs after 3 failed single-shot attempts:
   - Vol.1 — DONE, verified (`domains/brahma-sphuta-siddhanta-vol1.md`, 316 lines, no placeholder text)
   - Vol.2 — **IN FLIGHT** as of this handoff: `job_mu3ysir9` (Hermes async, dispatched ~06:2x EDT). Check with `mcp__hermes__check_hermes_job(job_id="job_mu3ysir9")`, then verify `domains/brahma-sphuta-siddhanta-vol2.md` exists with `wc -l` and grep for "Extracted description"/"Hypothetical Structure" placeholder text (zero matches required) before trusting it.
   - Vol.3 — NOT STARTED
   - Vol.4 — NOT STARTED
   - After all 4 volumes are done and verified individually, merge into a single `domains/brahma-sphuta-siddhanta.md` (delete/ignore the stray wrong-path file left at `E:\ClaudeWorkSpaceAnthropic\pl0-app\domains\brahma-sphuta-siddhanta.md` — decoy path, not the real target, safe to ignore/delete).
7. Solar Arcs (Noel Tyl) — NOT STARTED. Target: `domains/solar-arcs-noel-tyl-REFERENCE-ONLY.md`. Must be flagged reference-only/tropical, not merged into sidereal criteria (per earlier explicit instruction).

### Job prompt template that works reliably (use this shape for Vol.3, Vol.4, and Solar Arcs)
- NO "model routing note" preamble — that caused 4/6 jobs to silently ignore the real task on the first batch attempt.
- Explicit exact target path with "write ONLY to paths starting with this exact prefix" + naming the stale decoy path to avoid (`E:\ClaudeWorkSpaceAnthropic\pl0-app\` — missing `CloneOfpl0app`).
- "Use ONLY the built-in read_file tool... do NOT write custom Python code (execute_code) to read files" — a self-authored Python path-quoting bug caused one full failed attempt.
- Explicit QUALITY BAR paragraph forbidding "[Extracted description]" placeholders and "Hypothetical Structure" table labels, and forbidding chopped mid-sentence quotes.
- "write the file INCREMENTALLY in several smaller sequential write/append calls" if truncation starts — this is what saved Hora Ratnam and Jataka Parijata Vol.2 after their first pass truncated mid-write.
- Verify every job on disk with `wc -l` + placeholder grep before trusting "done" status. Job status "done" does NOT mean the content is good — 2 of the "done" Brahma Sphuta attempts were garbage/near-empty despite reporting success.

### Hermes cost/fallback setup (all live, confirmed working)
- Config file: `C:\Users\drrag\AppData\Local\hermes\config.yaml` — this is the REAL active config (not `~/.hermes/config.yaml`, a red herring). Same for `.env`: real one is `C:\Users\drrag\AppData\Local\hermes\.env`, not `~/.hermes/.env`.
- `OPENROUTER_API_KEY` updated to the user's new $10-funded key (ends `...772a`).
- `fallback_providers` chain, tried only when primary (`upstage/solar-pro4:free` via nous, deliberately left unchanged — see reasoning below) fails:
  1. `google/gemini-2.5-flash` via openrouter (currently set — NOT the Lite variant; Lite was tried and failed twice on the hardest book, Brahma Sphuta Siddhanta, with placeholder garbage then an outright refusal, so bumped back to full Flash)
  2. `stepfun/step-3.7-flash` via nous (free)
  3. `meituan/longcat-2.0:free` via nous (free)
- Primary model deliberately NOT switched to paid — `hermes insights --days 7` showed ~218.5M tokens/week of routine usage; switching primary would burn $10/week on routine work for no benefit. Paid fallback only fires on actual failures, which is the real problem being solved.
- Real spend as of this handoff (verified via `curl https://openrouter.ai/api/v1/credits`): **$4.72 of $10 used**, $5.28 remaining.
- User expressed real frustration mid-session about cost/retry discipline — see `feedback_paid_retry_cost_discipline.md` in the memory system (persists across sessions): cap at 2 failed paid retries on one subtask before checking in with the user, don't push a 3rd blind retry.

### Explicit instruction from user this session
"wrap it up for today and resume when you are awoken... resume on my command when i return" — DO NOT auto-resume, DO NOT dispatch any further Hermes jobs, until the user explicitly says to continue in a future session. The in-flight `job_mu3ysir9` (Vol.2) will keep running server-side on Hermes regardless — check its result when resuming, but do not proactively start Vol.3/Vol.4/Solar Arcs without the user's go-ahead.

## UPDATE 2026-09-16 (new session): Vol.2 confirmed failed (bad OCR, not a bug), Vol.3/Vol.4 ruled out, Solar Arcs dispatched, fallback cost-order fixed

User gave explicit go-ahead to resume the book-ingestion thread and asked to conserve the remaining ~$5 OpenRouter balance for at least 3 more days.

**`job_mu3ysir9` (Brahma Sphuta Siddhanta Vol.2) — checked, did NOT succeed.** The job correctly refused rather than fabricating output: it read all 20,000 lines of the source `.txt` and reported the OCR is too garbled to extract real astronomical content from, then stopped and asked how to proceed. No file was written. This is not a prompt/model problem — see next finding.

**Vol.3 and Vol.4 ruled out before spending any more paid tokens.** Grepped both source `.txt` files locally (free, no LLM) for real-word density using common astronomical/English vocabulary (`planet|verse|translation|chapter|longitude|equation|degrees`): Vol.1 (the one that succeeded) hit 588 matches; Vol.3 hit 1; Vol.4 hit 0. Manually sampled several offsets across both 17K/18K-line files — consistently Devanagari (Hindi/Sanskrit) text that was OCR'd character-by-character into garbage Latin glyphs, not just noisy scan quality. This is unrecoverable by any LLM reading the `.txt` — it would need a proper Devanagari-aware re-OCR of the source PDF first. **Decision: do not dispatch Vol.3/Vol.4 to Hermes.** If a better-OCR'd or native-PDF source for these two volumes ever turns up, redo this word-density sanity check on it before dispatching.

**Real OpenRouter balance verified via API** (`curl https://openrouter.ai/api/v1/credits`): `$10` total, **`$5.15095148` used → `$4.85` remaining** — worse than the `$4.72` used / `$5.28` remaining the prior handoff stated, because the failed Vol.2 attempt burned ~$0.43 more via the paid fallback before giving up.

**Found and fixed a real cost leak: fallback order had the paid model FIRST.** `C:\Users\drrag\AppData\Local\hermes\config.yaml`'s `fallback_providers` list was `[google/gemini-2.5-flash (paid), stepfun (free), longcat (free)]` — meaning every routine free-tier hiccup on the primary model paid immediately instead of trying the two free fallbacks first. Reordered to:
1. `stepfun/step-3.7-flash` via nous (free)
2. `meituan/longcat-2.0:free` via nous (free)
3. `google/gemini-2.5-flash-lite` via openrouter (cheap paid — new, added as a budget middle tier)
4. `google/gemini-2.5-flash` via openrouter (full paid, now last resort only)

Verified live via `hermes fallback list`. Primary model unchanged (`upstage/solar-pro4:free` via nous) — same reasoning as before, most traffic is free-tier routine work.

**Solar Arcs (Noel Tyl) dispatched — `job_mu4101th`.** Source file sanity-checked first (226 real-word hits, clean readable prose on spot-check — this one OCR'd fine, unlike the Brahma Sphuta volumes). Target: `domains/solar-arcs-noel-tyl-REFERENCE-ONLY.md`, explicit instruction embedded in the prompt to flag it Western/tropical reference-only at the top of the file, same as the earlier decision. Not yet checked for completion — check with `mcp__hermes__check_hermes_job(job_id="job_mu4101th")`, then verify on disk with `wc -l` + placeholder-text grep before trusting "done", per established practice.

**Book ingestion status after this update: 6 of 7 original books done** (Parashar Code, Vishnu Bhaskar, Hora Ratnam, Jataka Parijata Vol.1 & Vol.2, Brahma Sphuta Siddhanta Vol.1), **Solar Arcs in flight**, **Brahma Sphuta Siddhanta Vol.2/3/4 permanently blocked on unrecoverable source OCR** (not a task to retry — needs a better source scan, out of scope for Hermes-reads-a-txt-file). Note all the `domains/*.md` output files are still untracked in git (`git status` shows them as `??`) — nobody has run `git add`/`git commit` on the book-ingestion output yet, only on the `www/index.html` yoga-engine commits.

**Not touched this update**: yoga engine (#189+), APK push clarification — user chose to prioritize the book-ingestion thread this turn.

## UPDATE 2026-09-16 (same session, continued): Solar Arcs done, 24 commits pushed to GitHub

**Correction on "ping"**: user meant push the code to GitHub and notify there/here, not build an actual signed Android APK on this machine. (I had briefly started downloading an Android SDK to prep for a real build — stopped and deleted it immediately once corrected; no APK build work has actually happened, that's still fully open per the "APK build/push — unresolved" section above.)

**Solar Arcs — done on retry, verified.** First dispatch (`job_mu4101th`) silently truncated before the actual task text ever reached Hermes (same bridge-truncation issue noted earlier in this file) — it just ran a few `ls` commands and asked "what's the task?", zero real cost. Retried once with a shorter, flatter prompt (`job_mu415c26`) — succeeded, fell through only the free fallback chain (nemotron→stepfun→longcat, never touched paid Gemini) confirming the fallback-reorder fix from earlier this session is working as intended. Verified on disk: `domains/solar-arcs-noel-tyl-REFERENCE-ONLY.md`, 348 lines, zero placeholder-text matches, correct bold Western-tropical/reference-only warning at the top, 9 real worked chart examples with verbatim quotes.

**Book ingestion thread is now complete** given the Vol.2/3/4 OCR ruling: 6 of 7 originally-requested books done (Parashar Code, Vishnu Bhaskar, Hora Ratnam, Jataka Parijata Vol.1 & Vol.2, Brahma Sphuta Siddhanta Vol.1) plus Solar Arcs = 7 domain files total; Brahma Sphuta Siddhanta Vol.2/3/4 permanently blocked on unrecoverable source OCR (see above). All 7 `domains/*.md` files are still untracked in git — not committed/pushed, only the `www/index.html` code changes have been.

**Pushed 24 local commits to `origin/main` on GitHub** (`057e3f4..9aca737`): Directives A/B/C, Trikālagnāna fix, standalone Shadbala module, and Raman's 300-yoga engine batches 1–21 (185/300 implemented). Verified clean fast-forward (no divergence from origin) and scanned the diff for secrets before pushing — none found.

**Final OpenRouter balance this session**: $10 total, **$5.43 used, $4.57 remaining** (the truncated-prompt job cost effectively nothing; the successful Solar Arcs retry added ~$0.28, mostly/entirely on free-tier fallback models per the log).

**Still open, unchanged**: yoga engine from #189 onward, and the actual APK build (SDK install + debug-vs-signed-release decision + keystore) — nothing done there this session, don't assume otherwise from the aborted SDK-download tangent above.

## UPDATE 2026-09-16 (same session, continued): New 5-task UI/content backlog — do AFTER yoga engine, before any APK push

User reviewed 5 live screenshots of the app's main/results page and gave a new backlog. **Explicit sequencing: finish "the current yoga tasks" (the Raman 300-yoga engine) FIRST, then do these 5 tasks, and do NOT push/build the APK until both are done.** Explicit execution instruction: delegate tasks 1-4 to Hermes and/or opencode (confirmed installed, `SST.opencode` v1.18.21 via winget) — "try not to use yourself [Claude directly] except for the last task" (task 5). The yoga-engine continuation is NOT covered by that restriction (it's referenced as separate, already-established work) — kept doing that the same way as before (direct Edit-tool + jsdom verification).

**Code locations confirmed for all 5 (via grep, 2026-09-16), all currently live inside `#results` / `runAnalysis()` — i.e. the main "Cast & Judge Muhūrta" home page, not a sub-page:**

1. **Quote/blessing footer** ("Though my soul may set in darkness..." — Sarah Williams, "The Old Astronomer" — + the samayavidya blessing + disclaimer) — `www/index.html:15243-15248`, inside `rerenderChartPages()` (~L15114). **Task: remove entirely.**
2. **Pañcaka Vedha breakdown table + Nāḍī of the reference star panel** — `www/index.html:8152-8180`ish, inside `runAnalysis()` (starts L7788), which renders the main home page. **Task: remove from here entirely, move to a different page.** No destination specified by the user — I defaulted to proposing `pageMuhurta` (thematically the closest existing page — day/moment-quality detail) but flagged this as my own call, not confirmed. Confirm/redirect before Hermes builds this.
3. **"Functional benefic / malefic by house lordship" table** — `www/index.html:9480`ish, inside `renderNatalPage()` (starts L8334). **Task: keep the table, add a user-friendly interpretive description of what each lordship placement actually means/affects** — sourced from the `domains/*.md` corpus (Hermes should search it, not invent from scratch).
4. **"Ashtakavarga — Transit Strength" page** — `www/index.html:10309` (`// ---- Ashtakavarga — Transit Strength ----`), `renderAshtakavargaPage()` starts L10318, hint paragraph at L10365-10367. **Task: remove the long top explanation paragraph entirely; in the tables below (bindus-per-graha, Sarvāṣṭakavarga-by-sign), add real interpretive text about what each transit result means** — generated from the yoga/house/lordship/graha/sign/nakshatra formulations already in this app's data, but specifically for **transits** (gochara) — Hermes/opencode should search the full `domains/*.md` corpus for gochara/transit-specific classical results (most of the ingested books likely have a transit/gochara chapter — this hasn't been checked yet).
5. **Vimshottari Dasha page** (`Current Daśā stack`, zoom-depth 1-5) — `www/index.html:10449` (`renderDashaPage()`), stack table ~L10497-10502. **Task: synthesize real dasha-period interpretive descriptions** (per Mahādaśā/Antardaśā/etc. lord combination) from **all** the ingested classical works in `domains/*.md` — this is the one task the user wants Claude to do directly, not delegate, given the cross-referencing/synthesis judgment involved.

**Blocker found and reported to the user on task 5's originally-suggested source, `E:\PL9`:** this is an installed copy of commercial software "Parashara's Light 9" (GeoVision Software). Checked three different file types inside it — `Res1/Dashaeff/Dashahd.txt1` (the actual dasha-effects text database), `Res1/Description/Graha/*/[Graha]_Significations Dasha.html` (518 total description files across Graha/Nakshatra/Bhava/Drekkana/Karana/Lagna), and even `classics/Bphs/Bphs01.txt` (nominally a public-domain BPHS text) — **all are encrypted/obfuscated by the vendor**, not plain text despite some having `.txt`/`.html` extensions. This is deliberate content protection on GeoVision's part. **Decided not to attempt decryption/reverse-engineering this** — it's the vendor's proprietary paid content regardless of the underlying classical material's own public-domain status, and bulk-extracting it for reuse in this app would be a real copyright/anti-circumvention problem, not a technical inconvenience to route around. Recommended relying on the already-ingested public-domain `domains/*.md` corpus for task 5 instead, which is exactly what that corpus exists for. Awaiting user's decision on how to proceed with task 5's sourcing given this.

**Next when resuming**: continue yoga engine from #189 (see the immediately-preceding update section for the full reusable-helper list and verification discipline) — a fork was dispatched this session to continue that work in the background; check for its completion notification before assuming stale state here. Once the yoga engine reaches a real stopping point, move to drafting the actual Hermes/opencode prompts for tasks 1-4 (not yet drafted or dispatched — sequencing said after the yoga engine, so nothing has been sent to Hermes/opencode for this backlog yet), and revisit task 5's sourcing question with the user.

## UPDATE 2026-09-16 (same session, continued): ALL 5 UI/content backlog tasks done, verified, pushed — this thread is complete

Yoga engine finished first as required (see the "ALL 300 RAMAN COMBINATIONS" section above — 290/300 accounted for, 10 permanent documented gaps). Then all 5 backlog tasks done in order, tasks 1-4 delegated to Hermes and independently verified/fixed before commit, task 5 done directly per the user's own instruction. **All commits pushed to `origin/main`** — current HEAD `7b196fd`.

1. **Quote/blessing footer removed** (`0275646`). Trivial, clean diff, no issues.
2. **Pañcaka Vedha + Nāḍī panel moved from the main page to the Nakshatra page** (`6c7c049`). My original guess at destination (pageMuhurta) was wrong — that page turned out to already BE the main page; retargeted to pageNakshatra, which already reuses the same `getReferencePanchaka()`/`refP` pattern. Caught and fixed a real content corruption before committing: Hermes retyped "least restful" as "least restless" in one paragraph (an actual meaning inversion, self-contradicted two sentences later) — always diff-review Hermes's text moves, not just structural checks. Also found (not fixed) a pre-existing bug, confirmed via testing against the pre-move commit: `getReferencePanchaka()` throws if a fresh visitor opens the Nakshatra page before entering birth data.
3. **Interpretive text added to the "Functional benefic/malefic by house lordship" table** (`35708cc`). New `functionalLordshipReading()` reuses the existing `GRAHA_KARAKA`/`HOUSE_SIGNIF` tables (same voice as `vargaPlanetReadingLine`), correctly declined to force a domains-corpus citation after finding only a tangential Yogakaraka mention. Verified: 504/504 combinations clean.
4. **Ashtakavarga transit page rewritten** (`87c3946`). Long top explanation removed (panel title kept), new `ashtakavargaTransitReading()` added per row, same reuse pattern as task 3. This job took 11 minutes (mostly it debugging its own jsdom test script) and left a real defect my own diff review caught: the new function got dropped mid-sentence inside an existing multi-line `//` comment, splitting "A second," from "independent classical system..." — not a functional break (each line is still its own comment) but genuinely confusing; fixed by moving the function after the full original comment. Verified: 252/252 combinations clean afterward.
5. **Classical Mahādaśā effects added to the Dasha page — done directly by Claude, not delegated** (`7b196fd`), per the user's own instruction that this one task needed real cross-referencing judgment. Real find: the available-skills list includes ~1050 raw, unprocessed jyotish book PDFs (pointer-stub skills under `C:\Users\drrag\.claude\skills\`, each with a `pdftotext` extraction command) — a far bigger source pool than the 9 `domains/*.md` files. Extracted and verified Shanker Adawal's *Encyclopedia of Vedic Astrology: Dasa Systems* (18,553-line `pdftotext -layout` extraction), pulled genuine "Effects of the Major Period of [graha]" content with strong/weak branches for all 9 grahas, cited with txt-line numbers in new `domains/mahadasha-effects-adawal.md`. Wired in as `MAHADASHA_CLASSICAL_EFFECTS` + `mahadashaClassicalReading()`, gated by the Mahādaśā lord's own natal dignity via the existing `dignityTierInSign()` — a genuinely separate, complementary layer to the pre-existing `dashaLordReading`/`dashaFunnelNote` mechanism (that one derives from this app's own Lagna-based lordship engine; this is the source's own stated effects). Verified: 108/108 graha×sign combinations clean, full page boot + all 5 zoom levels cycled with zero errors.

**Task 5 sourcing dead ends, both declined on purpose, not revisited**: `E:\PL9` (Parashara's Light 9, GeoVision Software) has its whole content library — including the bundled BPHS text — encrypted by the vendor; a second folder the user pointed at (`...\Upsidedown\hereitis\bosesesalter\`, and a further stray copy that ended up at the repo root as `Here/`, harmless clutter, left alone) was confirmed byte-identical via md5sum to the same encrypted PL9 files, not a different source. Not decrypting either — vendor's proprietary content, real copyright/circumvention concern, independent of the underlying classical material's own public-domain status.

**Documented, not built this session**: the same Adawal source has a much larger second pass (~4000 lines, txt-line ~11800-16200+) of generalizable Antardaśā (sub-period) rules keyed to the sub-period lord's house position *from the Mahādaśā lord*, using a "Gochar/Agochar" dignity concept (own/exalted/friend sign, not eclipsed, not in 6th/8th/12th from the main-period lord = Gochar/good; the inverse = "Yam Gochar"/bad) that doesn't exist anywhere else in this app. Genuinely valuable, genuinely out of scope for this pass — flagged in `domains/mahadasha-effects-adawal.md`'s own closing section for whoever picks it up next.

**Also fixed this session, unrelated to the 5-task backlog**: swapped Hermes's paid OpenRouter fallback from `google/gemini-2.5-flash(-lite)` to `qwen/qwen3.7-flash` at the user's request — verified it's a real model on OpenRouter's live catalog (`$0.03/M in, $0.13/M out` base tier, meaningfully cheaper). Fallback chain is now `stepfun (free) → longcat (free) → qwen3.7-flash (paid, last resort)`, confirmed live via `hermes fallback list`.

**Not touched, still fully open**: the actual APK build (SDK install, debug-vs-signed-release decision, keystore) — see the "APK build/push — unresolved" section near the top of this file, nothing there has changed. Per the user's explicit instruction this session, do not build/push the APK until they say so, now that both the yoga engine and this 5-task backlog are done.

## UPDATE 2026-09-16 (later session): APK actually built and released; a Vaiseshikamsa cumulative-ladder bug found via user's own chart; a large second yoga-engine expansion now queued

User gave the go-ahead on the APK. Built a real debug APK end to end: installed the Android SDK fresh on this machine (none existed before -- cmdline-tools, platform-tools, platform 36, build-tools 36.0.0, all under `E:\ClaudeWorkSpaceAnthropic\android-sdk`), fixed a Windows path-escaping bug in `android/local.properties` (colon needs escaping, forward slashes not backslashes), ran `npx cap sync android` (first sync ever -- the assets folder didn't exist before this), then `gradlew assembleDebug`. Verified the resulting APK myself (not just "build succeeded"): `unzip -t` for zip integrity, `aapt dump badging` for package name/version/SDK targets, `apksigner verify` for a genuine debug signature. Published as a GitHub Release: https://github.com/pbresta-byte/pl0-app/releases/tag/debug-apk-2026-09-16. Flagged a real pre-existing issue in the release notes: git tags go up to `v1.65` but `android/app/build.gradle`'s versionCode/versionName are still stuck at 156/1.56 -- never bumped in sync. Not fixed, just documented.

**Real bug found and fixed via the user's own insistence on their own chart (Trikalagnana Yoga):** user's real birth chart (Jupiter at 14.63 Cancer, matches `_cristian_chart.json` and the app's own DEMO_BIRTH default) should satisfy Trikalagnana's Gopuramsa condition but the app said absent. Investigation found two real bugs, both now fixed (commit `17b6713`): (1) a prior session (2026-09-15) had misread B.V. Raman's primary text, hardcoding one illustrative worked example ("Suppose Jupiter is in the first degree of Dhanus...") as if it were an exhaustive restriction to only D1+D3+D4+D7, when the actual rule is general ("occupies his own varga four times," any four); (2) more importantly, this app's named Vaiseshikamsa dignity tiers (Gopura, Uttama, Simhasana, Parvata, etc.) form a CUMULATIVE ladder -- reaching a higher count already clears every lower threshold -- but several yogas were checking `count===N` (exact match) instead of `count>=N`. User's own Jupiter is at Dasavarga count 5 (Simhasana), one rung above Gopura(4), so the exact-match bug wrongly reported it as failing. Grepped the whole 290-yoga engine and fixed 5 separate instances of this same bug (Trikalagnana's Gopuramsa, #155's Parvatamsa/Simhasanamsa, #167's Gopuramsa, #206's Parvata/Uttamamsa, #256's Uttamamsa), not just the one under dispute -- verified via jsdom against the real chart plus a second unrelated chart to confirm the fix doesn't cause universal false positives. **Lesson for future sessions, saved to memory ([[feedback-rigor-not-reassurance]]):** when this user insists a calculated result is wrong, that has now been correct twice on real, verifiable bugs -- always re-derive from the primary source text and be willing to reverse a prior session's own conclusion.

**Claude Artifact now kept live for this app**, per explicit user request (they were frustrated this wasn't already standard practice): https://claude.ai/artifact/MKAgxdtL2iAFnKsu2fFc6r ("PL0 — Sarvato Bhadra Chakra Analyzer"). Republish to that same URL after any real change to `www/index.html` going forward, don't wait to be asked -- see [[project-pl0-app]] memory for the exact strip-and-republish recipe (the Artifact tool forbids the page bringing its own `<html>/<head>/<body>`, but `www/index.html` is a full standalone document, so a stripped copy of just the inner content gets published, never the real repo file).

**Fourth batch of parallel work dispatched this session** (independent files, no conflicts, all via Hermes with Claude auditing -- explicit user instruction: "Hermes as the brain, you as the final auditor, make sure it doesn't fall asleep"):
1. **pl0-app Yoga panel restructure** (www/index.html): remove the separate "Raja Yoga — power combinations" and "Jñāna Yoga series" sub-headers, fold Raja Yoga's pairs/dualRulers output into the same positive/negative/mixed polarity-grouped list as the Jnana yogas (one unified list, three possible colored headers, no more separate sections). checkRajYoga's underlying calculation (Kendra-lord + Trikona-lord connection = Raja Yoga) was evaluated and judged NOT to need deletion despite lacking a specific verse citation like the rest of the 300-engine -- it's one of the most foundational, universally-cited Parashari principles (BPHS, Phaladeepika, Saravali all state it), just under-cited relative to this project's own internal standard; instructed to cite it honestly as a cross-source foundational principle rather than inventing a fake specific verse number. Needed 2 retries due to the recurring Hermes prompt-truncation bug (same known bridge quirk, fixed each time with a shorter flatter prompt) -- status when this note was written: still in flight, check job outcome and verify independently (diff review + jsdom) before trusting, same discipline as everything else in this file.
2. **Classics research for a second yoga-engine expansion** (research only, no edits): found and ranked three strong candidates via real web research (not just the already-ingested domains/*.md corpus) -- Phaladeepika ch.6 (~80+ named yogas, same computable shape as Raman's 300, highest priority), BPHS's 144 Bhava Yogas (12x12 house-lord-in-house matrix, zero current coverage, would be a new module not an extension), Saravali ch.16-20 (conjunction-set oracle, 119 combinations of 2-6 planets together in one house, a genuinely different condition-shape than named yogas). Also flagged: the user's local "Hora Sara" text chunks (`E:\ClaudeWorkSpaceAnthropic\AllBooks\Hereitis\Sara\SARA00.TXT` etc., NOT the readable `SARA__.TXT` table-of-contents file in the same folder) are the SAME GeoVision/PL9-encrypted content already declined earlier this session (same Bphs/CHAP/Garga/Hora/Sara folder-naming pattern) -- not a fixable text-encoding bug, don't attempt to "solve" it in a future session.
3. **User's decision on the above: ALL THREE candidates, plus add a dispositor-chain-based yoga system too** ("add dispositers too the engine and the yogas associated with them for user calculations"). This is now a large, multi-session second-phase project comparable in scope to the original 300-yoga build-out -- **not started yet as of this note**, queued to begin after the current in-flight batch (item 1 above, and item 4 below) finishes, verifies, and is pushed. Per the user's own explicit pacing instruction ("distribute your tasks in a relaxed fashion, bit by bit since no paid models are currently being used"), this should be worked through incrementally in small batches the same way Raman's 300 was, not attempted all at once.
4. **MT4 indicator work, a completely separate codebase from pl0-app** (`C:\Users\drrag\AppData\Roaming\MetaQuotes\Terminal\7E6C4A6F67D435CAE80890D8C1401332\MQL4\Indicators\`): (a) ported the user's TradingView Pine Script v6 "Price Hot Zone" indicator (source at `C:\Users\drrag\OneDrive\Desktop\Latest91626Tradingviewpinescript.txt`, a pivot-scoring buy/sell-zone marker) to a new `PriceHotZone.mq4` -- **done, bug found and fixed.** Hermes's port was structurally faithful but had a real, critical bug caught only by manual audit: it used the OnCalculate array parameters (open[]/high[]/low[]/close[]/time[]) assuming index 0 = current bar (matching both Pine's own convention and this account's other indicator's convention), but never called `ArraySetAsSeries()`, so MQL4's default oldest-first indexing would have silently run the whole respect%-backtest backwards and anchored the zone labels at the oldest bar in history instead of the current one. Fixed and verified (brace/paren balance, `ArraySetAsSeries` confirmed present for all 5 arrays). (b) enhancing the user's existing `WyattsPivots_v3.mq4` so that when the CHART itself is already on Monthly (MN1) view, instead of the current fallback (plain MN1 pivots again, redundant with the bars already on screen), it computes synthetic quarterly (3-month) and semi-annual (6-month) pivots by aggregating consecutive MN1 bars manually (MT4 has no native timeframe constant for either period) -- **still in flight as of this note, longest-running of the four dispatched jobs (likely due to the GetPivotPoints refactor it was asked to do), check job outcome and verify independently before trusting, same discipline as (a) above.**

**Task 1 (Raja Yoga / Jnana Yoga unification) result, for the record:** done and pushed, commit `4fd574b`. Architecture was sound (new `buildRajaYogaEntries()` correctly converts checkRajYoga's pairs/dualRulers into JNANA_YOGAS-shaped entries, concatenated before the existing filter/group pipeline runs), but independent review caught a real language-purity bug before committing: the Spanish gloss/reasonLine text was built using `ordinalSuffix()` (English-only, "7th") and raw untranslated graha names inside what were supposed to be Spanish strings. Fixed using this codebase's own established idioms (`GRAHA_ES[g]` for graha names, `casa N` instead of a translated ordinal for Spanish house references -- matching the exact pattern already used elsewhere in this file for the "Rules (from Lagna)" column). Verified via jsdom in both languages before pushing.

**Task 5 sourcing note**: user pointed at a second folder, `C:\Users\drrag\OneDrive\Desktop\Upsidedown\hereitis\bosesesalter\` (Bphs/CHAP/Garga/Hora/Sara subfolders), thinking it might be different/plain-text source material. Checked — `md5sum` confirms it's byte-identical to the files already inside `E:\PL9\classics\Bphs\`, i.e. the exact same GeoVision-encrypted content, just a second copy in a different location. Does not change the task 5 blocker/decision from the entry above. Not decrypting either copy.

## UPDATE 2026-09-16 (same session, continued): yoga fork batch 1 done — 215/300, second fork dispatched

First background fork (batches 22-24) reached a clean stopping point at **215/300** and reported back rather than running indefinitely — this was a "natural checkpoint" stop, not "done with all 300." Commits `4628cac`/`a665592`/`acedd5c` verified clean (fast-forward, no secrets) and **pushed to origin/main** (`9aca737..acedd5c`).

**One real skip this batch**: **#204** (a Kapata Yoga) needs 4th lord + Saturn + Mandi + Rahu all together (an AND clause) — same missing-Mandi/Gulika gap as #11/#174, and since it's an AND (not an omittable OR-branch) it's a genuine skip, noted in intro text only.

**Judgment calls worth a second look later** (flagged by the fork itself, not fully resolved):
- **#206**: mapped Raman's "Parvata or Uttamamsa" named tiers onto this app's existing Vaiseshikamsa table's "Uttama"(3)/"Paaraavata"(6), treating "Paaraavata" as a transliteration variant of "Parvata" — no worked chart to verify against, flagged as an assumption in the code comment.
- **#219**: kept the "weak Moon" clause on the standard own/exalted/friend dignity convention, but Raman's own narrative describes the Moon becoming "subject to a series of afflictions" from other planets in the combination — might mean something more like independently-afflicted rather than dignity-based. Flagged, not resolved.
- **#214** is a genuine worked-chart counter-example (Jupiter exalted+strong renders the yoga "defunct" despite every other condition holding) — implemented via the dignity-tier proxy since Shadbala stays unwired; worth revisiting if Shadbala ever gets wired in for real.
- **#216** is the most structurally complex entry so far (six independently-OR'd branches under one number) — all six implemented, flagged for a careful second read.

**Tooling note for future jsdom verification**: drop `resources:'usable'`/`pretendToBeVisual` from the JSDOM options and don't pipe the verification script through `tail` — both caused a ~10-minute false hang this run (event loop never drained; `tail` buffers until EOF). Plain `new JSDOM(html, {runScripts:'dangerously'})` + explicit `dom.window.close()` + `process.exit()` at the end runs in seconds.

**Next entry: #220**, raw text starts at line 8397 of the source txt (still in the "loss of children" thematic cluster — expect several more entries in this vein before a topic change). Second continuation fork dispatched immediately after this checkpoint to keep pushing toward 300 — check for its own completion notification.

## UPDATE 2026-09-16 (same session, continued): ALL 300 RAMAN COMBINATIONS NOW ACCOUNTED FOR — this thread is done

The second continuation fork (this one) kept going all the way to the end of the book instead of stopping at another mid-way checkpoint. **Final result: 289 of 300 entries implemented directly in `RAMAN_YOGAS`, plus #234 (Thrikālagnāna) already implemented separately in the Jñāna Yoga panel = 290 of 300 accounted for.** The remaining 10 are genuine, permanently-documented gaps (not oversights) — every one of them needs a capability this app doesn't have, and every one is named in the panel's own intro-text hint rather than silently skipped:

- **#11, #174, #204, #264, #270** — need Mandi (a Gulika-family zodiacal point). Never computed anywhere in this app, across the whole project. If Mandi ever gets built, these five become trivial to add.
- **#185, #273** — need a verified cruel/benefic classification of the 60 D60 (Shashtiamsa) divisions. No verified source table exists in this project; this app does have a numeric D60 *ordinal* (`mridvamshaOrdinal`), but not a deity/temperament table.
- **#276, #277** — need an "evil sub-division/amsa" classification. Unlike the D60 case above, the source itself never names a specific classical system here — too vague to operationalize even in principle, not just unverified.
- **#297** — needs a real Aroodha Dwadasa (A12) calculation plus a reliable Aroodha Lagna (A1). **Found and flagged a likely pre-existing bug**, not fixed here (out of scope for this task): this app's only Arudha function, `computeArudhaLagna()` (`www/index.html:8315`, used by the House Nature page), takes ONLY the Ascendant's own degree as input — it never receives the Lagna lord's position at all. Raman's own text, two paragraphs before yoga #297, states the real classical formula plainly: "Aroodha Lagna is the sign arrived at by counting as many signs from Lagnadhipathi as the Lagnadhipathi is removed from Lagna" — a formula that structurally *requires* knowing where the lord is. Since the existing function never looks at the lord's position, it cannot be computing this correctly. **Someone should audit/fix `computeArudhaLagna()` as its own task** — this is a correctness bug in a shipped, user-visible feature (the House Nature page), independent of the yoga-engine work. No Aroodha Dwadasa exists anywhere in this app yet either.

**Total commits this run** (all local, NOT pushed — parent instruction was explicit: leave commits local, they'll push later as part of a larger batch): `4628cac` (batch22) → `a665592` (batch23, already pushed earlier as part of a prior checkpoint) → `acedd5c` (batch24, already pushed) → `9a8d608` (batch25, #220-223) → `de816b7` (batch26, #224-238) → `563cf84` (batch27, #239-244) → `796caee` (batch28, #245-263, full Raja Yoga chapter) → `9039b36` (batch29, #265-279) → `25db30c` (batch30, #280-290) → `2c473a4` (final batch, #291-296+#298-300). Verify with `git log --oneline -12` before assuming this is fully up to date.

**Judgment calls from this final stretch worth a second look** (flagged in code comments, not silently resolved):
- **#278 (Peenasaroga)**: implemented as the literal 4-clause Definition, but Raman's own worked chart for it only satisfies 3 of the 4 clauses (missing the Lagna-lord malefic-Navamsa clause) and his own prose elsewhere emphasizes a *different* 2-factor subset than what that chart shows. The source is genuinely self-inconsistent here — no clean alternative structure to switch to, so the literal Definition was kept.
- **#287 (Pisacha Grastha)**: "malefics join trines" has no worked example to pin down which specific malefic(s)/which specific trikona house(s) — operationalized loosely as "any of Sun/Mars/Saturn in the 5th or 9th," an honest guess, not a verified reading.
- **#275 (Yuddhe Marana)**: implemented via Raman's own explicitly-confirmed alternate path (a Drekkana-lord check with a worked chart behind it) rather than his primary Definition, which needs the same unverified cruel-amsa classification as #273/#185.

**Reusable additions from this final stretch**: `isMoonWaxing`-style inline waning check (`norm360(pos.Moon.sidereal - pos.Sun.sidereal) >= 180`, used ad hoc in #281/#293/#296 rather than promoted to a shared helper since only 3 call sites so far), `isCombust`/`hasDigbala`/`isNaturalFriendGraha` (added in the Raja Yoga chapter, batch 28, reusable for any future addition), `drekkanaSignIdx()` reused directly from the existing D3 varga code (not new, but newly load-bearing for a yoga's core logic for the first time via #275).

**What's next for this project, per the user's own stated sequencing** (see the "New 5-task UI/content backlog" section above, dated earlier 2026-09-16): the yoga engine was the explicit blocker before touching the 5-task UI/content backlog (remove quote footer, move Pañcaka Vedha panel, add interpretive text to the house-lordship table, rewrite the Ashtakavarga transit page, and the Dasha-period synthesis task) and before any APK build/push. **That blocker is now cleared.** Whoever resumes next should: (1) push these 10 local commits to `origin/main` (run the usual clean-fast-forward + secret-scan check first), (2) confirm with the user whether "the current yoga tasks" being done means proceed straight to the 5-task backlog now, and (3) if fixing `computeArudhaLagna()` is wanted, treat it as its own small task, not bundled into whichever of the 5 UI tasks comes first.

## UPDATE 2026-09-16 (same session, continued): Trikālagnāna display-text fix + Jaimini Ātmakaraka feature added

Two changes, both directly by me (not delegated), verified via the established jsdom discipline.

**1. Trikālagnāna gloss/reasonLine text was stale relative to the fixed function.** During an unrelated pass through the Yoga panel code (~line 8421-8426), found that `trikalagnanaYoga`'s own logic had already been correctly fixed (per the earlier "gopuramsha count>=4" bug-hunt, see prior UPDATE) but the user-facing `glossEn`/`glossEs`/`reasonLine` text shown on the Yoga panel still described the OLD, wrong reading — "own sign in Rasi, Drekkāṇa, Chaturthāṁśa AND Saptāṁśa all at once" (the narrow D1+D3+D4+D7-only interpretation the user explicitly rejected). Fixed the display text to describe the actual current logic (Vaiśeṣikāṁśa Daśavarga count>=4, cumulative ladder), and changed `reasonLine` to print the real count (`r.gopuramsha.count`/10) instead of a boolean against the old wrong description. This was a display-only bug — the underlying calculation was already correct — but leaving the wrong explanation on screen would have looked like the bug was never fixed.

**2. New feature: Jaimini Chara Karakas / Ātmakaraka**, per explicit request (prompted by a JHora screenshot of Ramana Maharshi's chart) to add "the general Jaimini atmakaraka system not the rahu/ketu considered one." Implementation:
- `computeAtmakaraka(pos)` (`www/index.html`, added right after `GRAHA_KARAKA`, ~line 10433) — standard 7-graha scheme (Sun–Saturn only), ranks by degree-within-own-sign descending; rank 1 = Ātmakāraka down to rank 7 = Dārakāraka. `JAIMINI_KARAKA_GRAHAS` / `JAIMINI_KARAKA_NAMES` constants added alongside.
- New "Jaimini Chara Karakas" panel on the Natal page, inserted right after the D1/D9/Ashtakavarga grid (before the Functional benefic/malefic panel). Shows the full 7-karaka table (karaka name, graha, signification, D1 position, house-from-Lagna), the Karakāṁśa (Navāṁśa sign occupied by the Ātmakāraka — computed by reusing the page's existing `navPos`), and an explicit hedge note about the alternate 8-karaka Rāhu-inclusive variant per the user's own requested wording: this app does not compute that variant, and the "generally for saints" claim floating around some modern sources has no rigorous primary-source citation found — flagged as a noted tradition, not a verified classical rule.
- Verified via jsdom: booted the app, rendered the Natal page, confirmed no runtime errors, no `undefined`/`NaN` leakage, correct 7-graha strictly-descending ranking with Rahu/Ketu absent, and no English-text leakage in the Spanish render. Sanity-checked against the user's own DEMO_BIRTH chart: Mercury comes out as his Ātmakāraka (28.25° in its own sign), which is plausible and matches the existing intuition that Mercury dominates his placements table.

**Also delivered (not code, conversational)**: a full narrative astrological reading of Ramana Maharshi's chart per the user's request, grounded in the actually-legible screenshot data (Shadbala table, Vaiśeṣikāṁśa tiers, JHora's own detected yoga list), while being explicit that exact house/degree placements couldn't be read reliably from the screenshot's diamond-chart layout alone — offered to compute a precise version if given the real birth data.

**Still pending** (see prior UPDATE's "what's next" list — unchanged by this update): (a) verify the still-running `WyattsPivots_v3.mq4` quarterly/half-yearly Hermes job, (b) the second-phase yoga-engine expansion (Phaladeepika ch.6, BPHS 144 Bhava Yogas, Saravali ch.16-20, dispositor-chain yogas) — user said to pace this "bit by bit," (c) the original 5-task Natal-page UI/content backlog, (d) a finalized APK build+push as the explicit last step once everything above is done, (e) keep the Claude Artifact updated (recipe: strip outer `<!DOCTYPE html><html><head>`/`</head><body>`/`</body></html>` before publishing).

## UPDATE 2026-09-16 (same session, continued): Yoga panel merge, Ashtakavarga explainer, Hermes narrative-framework jobs dispatched

Five-part follow-up request from the user (with screenshots). Progress:

**1. Done — merged the separate "Raman's 300 Important Combinations" panel into the single "Yoga combinations" panel.** Removed the panel's own title, its "B.V. Raman's Three Hundred Important Combinations..." sourcing/description paragraph, and its separate active-count badge entirely, per explicit instruction ("remove title and description completely"). All three yoga sources (JNANA_YOGAS + Raja Yoga entries + all ~290 RAMAN_YOGAS) now feed one `masterYogaEntries` array, grouped into the same positive/negative/mixed sections as before — RAMAN_YOGAS entries keep their `#N` numeric tag inline (e.g. "#1 Gajakesari Yoga") since that's a legitimate cross-reference, not "sourcing" branding. Individual per-yoga glosses that happen to name "Raman" for provenance (~278 of them) were deliberately left alone — that's substantive citation, not the panel-level branding the user was objecting to; flagged this distinction in case the user meant something broader. New code: `masterYogaEntries`/`masterYogaActiveCount`/`masterYogaHtml` replace the old separate `jnanaYogaHtml`/`ramanYogaHtml`. Verified via jsdom: single merged panel, no duplicate group headers, `#`-numbered entries present, no runtime errors, clean Spanish render.

**2. Done, as a side effect of #1.** The stray "and Trikālagnāna" name-drop in the old panel's own summary/intro paragraph ("Ten classical yoga-formation rules... nine knowledge-and-insight yogas, and Trikālagnāna") is gone along with the rest of that removed intro text. Grepped the whole file afterward — the only remaining "Trikal" mention is the yoga's own card in the merged list, which is correct (it's an active yoga, not a stray reference).

**3. In progress — descriptive-narrative expansion for Dasha/lordship/transit/SBC.** Per the user's explicit request to route this through Hermes ("consult the jyotish board") and to pace things without burning credits, dispatched two small, narrowly-scoped async Hermes jobs rather than one combined one (past experience: combined multi-step Hermes jobs under-deliver):
   - `job_mu4lpxq6` — asked Hermes to read this app's existing narrative-synthesis functions (`vargaPlanetReadingLine`, `functionalLordshipReading`, `ashtakavargaTransitReading`) as house style, then write a design document only (`domains/narrative-synthesis-framework.md`) proposing how to deepen the Daśā, House Lordship, Ashtakavarga transit, and SBC muhūrta narrative text with real classical theory (BPHS/Phaladeepika/Saravali/Muhūrta classics), citations included, no code yet.
   - `job_mu4lq56v` — pure research: does a reliable primary source exist for the Ashtakavarga Śodhana (Trikoṇa/Ekādhipatya) "Rāśi variant" reduction the user asked about in task 4? Write findings (verified quote+citation, or an honest "not found" note) to `domains/ashtakavarga-shodhana.md`. No code in this job either.
   Next step once both return: review each job's output myself (never trust Hermes's own "done" claim), then dispatch narrow, one-area-at-a-time content jobs using the approved framework — starting with Daśā, the most naturally narrative of the four — verifying each via jsdom before moving to the next.

**4. Partly done — Ashtakavarga explainer.** Added a new "What Ashtakavarga measures" panel to the top of the Ashtakavarga page (`renderAshtakavargaPage`, before the existing Transit-check panel): explains the 8-reference-point bindu system, Bhinnāṣṭakavarga vs Sarvāṣṭakavarga, and — the part the user specifically asked for — what a bindu count actually *means* for a graha (high count = that graha's own significations find support when it transits/sits there; low count = strain, independent of the graha's own sign-dignity) versus what Sarvāṣṭakavarga means at the sign level. The "Rāśi variant" (Trikoṇa/Ekādhipatya Śodhana) is explicitly flagged as not yet implemented — no verified primary source ingested for its exact rule set yet — rather than guessed at; `job_mu4lq56v` above is chasing down whether one exists. Verified via jsdom, clean in both languages.

**5. Not started yet.** APK build/push deferred until the Hermes narrative-expansion work (item 3) actually lands and is verified — per the user's own stated preference, updating the Claude Artifact instead for now to reflect items 1 and 4.

**Commit for this update**: covers the yoga-panel merge (`masterYogaEntries`/`masterYogaActiveCount`/`masterYogaHtml`) and the Ashtakavarga explainer panel, both in `www/index.html`.
